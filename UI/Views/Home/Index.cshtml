@{
    ViewBag.Title = "Hello World!";
}
<h2>Hello World!</h2>
It appears that you @(HttpContext.Current.User.Identity.IsAuthenticated ? "are" : "are not")
authenticated @HttpContext.Current.User.Identity.Name
@if (HttpContext.Current.User.Identity.IsAuthenticated)
{
    <ul>
        <li>@Html.ActionLink("Do Stuff", "DoStuff")</li>
        <li>@Html.ActionLink("Sign Out", "SignOut")</li>
    </ul>
}